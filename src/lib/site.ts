/**
 * What the site calls itself and who writes it, defined once so that the tab
 * title, the nav bar, the feed, and the social cards can't drift apart.
 */

/** The site's one name, wherever it names itself. */
export const SITE_NAME = "Sal's Blog";

/** A one-line description of the site, not a second name for it. */
export const SITE_TAGLINE = "Thoughts from a programmer";

/** Who writes the blog. */
export const AUTHOR_NAME = "Sal Rahman";

/** Where the author's name links to. */
export const AUTHOR_URL = "https://salrahman.com";
